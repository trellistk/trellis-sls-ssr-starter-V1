{
  "$schema": "http://json-schema.org/draft-04/schema#",
  
  "type": "object",

  "title": "Reset Request password User schema",

  "required": [
    "email",
    "chapter"
  ],

  "properties": {
    "chapter": {
      "type": "string",
      "enum": ["seattle-eastside"]
    },
    "email": {
      "type": "string",
      "format": "email"
    }
  }
}