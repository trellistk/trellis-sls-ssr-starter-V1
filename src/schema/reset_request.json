{
  "$schema": "http://json-schema.org/draft-04/schema#",
  
  "type": "object",

  "title": "Reset Request password User schema",

  "required": [
    "email"
  ],

  "properties": {
    "email": {
      "type": "string",
      "format": "email"
    }
  }
}